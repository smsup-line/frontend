version: '3.8'

services:
  nextjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sfadmin-nextjs
    restart: unless-stopped
    ports:
      - "${APP_PORT:-6000}:6000"
    env_file:
      - .env
    networks:
      - sfadmin-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:6000', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.sfadmin.service=nextjs"
      - "com.sfadmin.environment=production"

networks:
  sfadmin-network:
    driver: bridge

